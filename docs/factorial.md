# Factorial

`factorial(n)` multiplica los enteros positivos hasta `n`. Por definición,
`factorial(0)` devuelve `1` y los valores negativos no son validos.

La funcion se usa como ejemplo para practicar diagnostico de regresiones.
