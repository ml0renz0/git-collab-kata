# Usage Guide

This project exposes a small calculator module in `app.calculator`.

Run the test suite before opening a pull request:

```bash
pytest -q
```

Use focused branches and keep each pull request scoped to one intention.
