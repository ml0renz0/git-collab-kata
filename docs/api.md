# API Notes

The calculator module exposes small arithmetic helpers for kata exercises.

Keep documentation changes focused and verify them with the local validator.

Prefer examples that match the behavior covered by tests.

When the branch history gets noisy, attach corrections as fixup commits.
